using UnityEngine;

public class AspectSetting : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Screen.SetResolution(540, 960, FullScreenMode.Windowed);
    }
}
